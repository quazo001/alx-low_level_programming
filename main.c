#include <stdio.h>

/**
  * main- A script that runs a C file through the preprocessor and save the result into another file
  * Return: Always (Success)
*/

int main(void) 
{
	 printf("Hello, world");
	 return (0);
}
